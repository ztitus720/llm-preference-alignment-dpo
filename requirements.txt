# Exact versions verified end-to-end (data build -> DPO train -> eval) on Colab T4.
# Pinned exactly rather than by range: Colab ships a preinstalled peft that
# satisfies a range like ">=0.11,<1", so pip leaves it in place and it then
# fails against a newer trl inside get_peft_model. These are the versions the
# notebook installs and verifies.
torch>=2.3
transformers==5.16.1
trl==1.12.0
peft==0.20.0
datasets==5.0.1
accelerate==1.14.0
bitsandbytes>=0.43  # only for 4-bit / 8-bit loading on very limited GPU memory
